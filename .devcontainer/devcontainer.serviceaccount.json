{
    "name": "Twingate-compatible Codespaces example (Service Account)",
    "dockerFile": "Dockerfile",
    "context": "..",
    "runArgs": [ "--cap-add=NET_ADMIN", "--device=/dev/net/tun" ],
    "postStartCommand": "sudo -E -- bash -c 'sed ''s/resolvconf//g'' /etc/resolv.conf | tee /etc/resolv.conf >/dev/null; /etc/init.d/dbus start; echo \"$TWINGATE_SERVICE_KEY\" | twingate setup --headless=-; twingate start'"
}